---
title: 'Process Bank Statements'
openapi: 'POST /parse/statements'
description: 'The statement processing endpoint allows you to extract data from uploaded bank statements. This is an asynchronous operation that returns a job ID for tracking the processing status.'
---

{/* The statement processing endpoint allows you to extract data from uploaded bank statements. This is an asynchronous operation that returns a job ID for tracking the processing status.

## Usage Examples

<CodeGroup>

```bash cURL
curl -X POST "https://api.invaro.ai/api/v1/parse/statements" \
  -H "Authorization: Bearer your_api_key" \
  -H "Content-Type: application/json" \
  -d '{
    "document_id": "doc_abc123"
  }'
```

```python Python
import requests

url = "https://api.invaro.ai/api/v1/parse/statements"
headers = {
    "Authorization": "Bearer your_api_key",
    "Content-Type": "application/json"
}
data = {
    "document_id": "doc_abc123"
}

response = requests.post(url, headers=headers, json=data)
print(response.json())
```

```javascript JavaScript
const response = await fetch('https://api.invaro.ai/api/v1/parse/statements', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer your_api_key',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    document_id: 'doc_abc123'
  })
});

const data = await response.json();
console.log(data);
```

</CodeGroup>

## Request Parameters

<ParamField body="document_id" type="string" required>
  Document ID obtained from the upload endpoint
</ParamField>

## Response Examples

### Success Response

```json
{
  "success": true,
  "data": {
    "job_id": "job_123abc",
    "status": "processing",
    "estimated_completion": "2024-01-07T10:05:00Z"
  }
}
```

### Error Response

```json
{
  "success": false,
  "error": {
    "code": "INVALID_DOCUMENT_ID",
    "message": "The provided document ID is invalid or expired"
  }
}
```

## Supported Banks

<Card>
Our system supports statements from major banks worldwide, including:
- Bank of America
- Chase
- Wells Fargo
- Citibank
- HSBC
- And many more...
</Card>

## Processing Details

The processing pipeline includes:
1. Document validation and preparation
2. OCR text extraction
3. Bank template detection
4. Data extraction and structuring
5. Validation and quality checks

## Next Steps

After starting the processing job:
1. [Check Job Status](/api-reference/endpoints/get-statement-status) to monitor progress
2. Review [Best Practices](/api-reference/best-practices/error-handling) for handling errors
3. Set up [Rate Limit](/api-reference/rate-limits) monitoring  */}