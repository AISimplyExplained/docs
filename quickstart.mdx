---
title: 'Quickstart'
description: 'Get started with Invaro API in minutes'
---

## Getting Started

Follow this guide to quickly integrate Invaro's document processing capabilities into your application.

### 1. Get Your API Key

<Steps>
  1. Sign up for an [Invaro account](https://dashboard.invaro.ai/signup)
  2. Navigate to the [API Keys section](https://dashboard.invaro.ai/api-keys)
  3. Create a new API key
  4. Store your API key securely - you'll need it for all API requests
</Steps>

### 2. Make Your First API Call

Test your API key by uploading a sample document:

<CodeGroup>

```bash cURL
curl -X POST "https://api.invaro.ai/api/v1/parse/upload" \
  -H "Authorization: Bearer your_api_key" \
  -F "files=@sample.pdf"
```

```python Python
import requests

url = "https://api.invaro.ai/api/v1/parse/upload"
headers = {
    "Authorization": "Bearer your_api_key"
}
files = {
    "files": ("sample.pdf", open("sample.pdf", "rb"))
}

response = requests.post(url, headers=headers, files=files)
print(response.json())
```

```javascript JavaScript
const form = new FormData();
form.append('files', fileInput.files[0]);

const response = await fetch('https://api.invaro.ai/api/v1/parse/upload', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer your_api_key'
  },
  body: form
});

const data = await response.json();
console.log(data);
```

</CodeGroup>

### 3. Process the Document

After uploading, process the document using the appropriate endpoint and the document ID from the upload response:

<CodeGroup>

```bash cURL
curl -X POST "https://api.invaro.ai/api/v1/parse/statements" \
  -H "Authorization: Bearer your_api_key" \
  -H "Content-Type: application/json" \
  -d '{
    "document_id": "doc_abc123"
  }'
```

```python Python
import requests

url = "https://api.invaro.ai/api/v1/parse/statements"
headers = {
    "Authorization": "Bearer your_api_key",
    "Content-Type": "application/json"
}
data = {
    "document_id": "doc_abc123"
}

response = requests.post(url, headers=headers, json=data)
print(response.json())
```

```javascript JavaScript
const response = await fetch('https://api.invaro.ai/api/v1/parse/statements', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer your_api_key',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    document_id: 'doc_abc123'
  })
});

const data = await response.json();
console.log(data);
```

</CodeGroup>

### 4. Check Processing Status

Monitor the processing status using the job ID:

<CodeGroup>

```bash cURL
curl "https://api.invaro.ai/api/v1/parse/statements/job_123abc" \
  -H "Authorization: Bearer your_api_key"
```

```python Python
import requests

url = "https://api.invaro.ai/api/v1/parse/statements/job_123abc"
headers = {
    "Authorization": "Bearer your_api_key"
}

response = requests.get(url, headers=headers)
print(response.json())
```

```javascript JavaScript
const response = await fetch('https://api.invaro.ai/api/v1/parse/statements/job_123abc', {
  headers: {
    'Authorization': 'Bearer your_api_key'
  }
});

const data = await response.json();
console.log(data);
```

</CodeGroup>

## Next Steps

<CardGroup cols={2}>
  <Card
    title="API Reference"
    icon="book-open"
    href="/api-reference/authentication"
  >
    Explore our detailed API documentation
  </Card>
  <Card
    title="Bank Statements"
    icon="file-invoice-dollar"
    href="/api-reference/statement-processing"
  >
    Learn about bank statement processing
  </Card>
  <Card
    title="Invoices"
    icon="file-invoice"
    href="/api-reference/invoice-processing"
  >
    Discover invoice processing capabilities
  </Card>
  <Card
    title="Best Practices"
    icon="stars"
    href="/api-reference/best-practices/error-handling"
  >
    Learn how to use the API effectively
  </Card>
</CardGroup>

## Support

Need help? Contact our support team:
- Email: [support@invaro.ai](mailto:support@invaro.ai)
- Dashboard: [https://dashboard.invaro.ai/support](https://dashboard.invaro.ai/support)
